qaTestModule.controller("qaTestHomeViewModel", function ($scope, qaTestService, $http, $q, $routeParams, $window, $location, viewModelHelper) {

    $scope.viewModelHelper = viewModelHelper;
    $scope.qaTestService = qaTestService;


    var initialize = function () {

    }

    initialize();
});
